# Harness Strategies

YAML files describing per-model `HarnessStrategy` configuration.

A strategy controls how various Harness components adapt to a specific
model:
- prompt framing
- tool call parsing
- context compression policy
- retry behavior

See `docs/components/H10-strategy-selector.md` for the schema.
