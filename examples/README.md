# Examples

End-to-end usage examples. These compile but are not part of the test
suite. Useful for showing patterns to future contributors.

Examples to add as the project grows:
- `simple_chat.rs` — minimum chat loop
- `replay_session.rs` — replay a session from its event log
- `custom_tool.rs` — register a custom tool
