# Scripts

Standalone scripts for experiments and benchmarks. Each script is a
self-contained Rust binary or shell script that produces data for
`docs/experiments/`.

Add a script here when you start a sprint experiment. Wire it into
`justfile` if it should be a first-class command.
