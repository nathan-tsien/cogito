//! cogito-cli library: exposes `chat_config` helpers and the `chat`
//! REPL module for integration tests. The binary entry point lives in
//! `main.rs`.

pub mod banner;
pub mod chat;
pub mod chat_config;
pub mod render;
